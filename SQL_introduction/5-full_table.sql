-- Print the full description of table first_table from database hbtn_0c_0 in MySQL server
-- We can also use \g or \G instead of ';' command terminator
    -- \g  go  Send command to mysql server
    -- \G  ego Send command to mysql server, display result vertically
SHOW CREATE TABLE first_table;
