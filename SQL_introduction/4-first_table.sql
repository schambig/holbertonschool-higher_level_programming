-- First be sure that the database hbtn_0c_0 exists (create it: task 1)
-- Create a table called first_table in the current database in MySQL server
CREATE TABLE IF NOT EXISTS first_table(
    id INT,
    name VARCHAR(256));
--    PRIMARY KEY(id));
